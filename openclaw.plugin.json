{
  "id": "super-agent",
  "name": "Super Agent",
  "description": "Tree-sitter Repo Mapper + Code Symbol Tool + Context Engine",
  "contracts": {
    "tools": ["read_code_symbol"],
    "contextEngines": ["super-agent"]
  },
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "projectRoot": {
        "type": "string",
        "description": "Absolute path to the project root (e.g., C:/projects/my-app). Auto-detected if empty."
      }
    },
    "additionalProperties": false
  }
}
